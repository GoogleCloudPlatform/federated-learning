apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-env-aggregator #kpt
  labels:
    app: aggregator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aggregator
  template:
    metadata:
      labels:
        app: aggregator
    spec:
      serviceAccountName: demo-env-gke-wi #kpt
      containers:
      - name: demo-env-aggregator #kpt
        image: europe-west1-docker.pkg.dev/lgu-demos/federated-learning-cross-device/aggregator@sha256:80c0aed96c9199cb4c42f97cd25d57a468944a71e6dbe752fc27ebf58373908b #kpt
        env:
          - name: "FCP_OPTS"
            value: "--project_id 'lgu-demos' --spanner_instance 'fcp-task-demo-env' --task_database_name 'fcp-task-db-demo-env' --client_gradient_bucket_template 'fcp-demo-env-g-%d' --aggregated_gradient_bucket_template 'fcp-demo-env-a-%d' --model_bucket_template 'fcp-demo-env-m-%d' --download_plan_token_duration 900 --download_checkpoint_token_duration 900 --upload_gradient_token_duration 900 --modelupdater_pubsub_topic 'modelupdater-demo-env-subscription' --aggregator_pubsub_subscription 'aggregator-demo-env-subscription'" #kpt
        securityContext:
          allowPrivilegeEscalation: false
          privileged: false
          readOnlyRootFilesystem: false
          runAsNonRoot: false
          capabilities:
            drop:
              - "NET_RAW"
        resources:
          requests:
            cpu: "2500m"
            memory: "16Gi"
            ephemeral-storage: "1Gi"
      securityContext:
        runAsNonRoot: false
        seccompProfile:
          type: RuntimeDefault
      tolerations:
        - effect: "NoSchedule"
          key: "kubernetes.io/arch"
          operator: "Equal"
          value: "amd64"